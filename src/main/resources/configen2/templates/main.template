// 这个配置文件是由 starter-configen 工具自动生成的。
// 任何时候，都不要手工修改这里面的内容！！！

package {{package.name}}

<import>

import (
	{{import.items}}
)

</import>

func autoGenConfig(configbuilder application.ConfigBuilder) error {

	cominfobuilder := &config.ComInfoBuilder{}
	err := errors.New("OK")

    {{cominfobuilder.items}}

	return nil
}

{{component.methods}}
